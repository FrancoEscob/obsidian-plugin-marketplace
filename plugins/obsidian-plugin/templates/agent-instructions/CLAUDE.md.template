<coding_guidelines>
# {{VAULT_NAME}} - Agent Instructions

**Generated by:** Obsidian Adaptive Plugin v1.0.0  
**Configuration:** {{CONFIG_NAME}}  
**Generated:** {{DATE}}  
**Vault Path:** `{{VAULT_PATH}}`

---

## 📖 READ THIS FIRST

This vault is configured with the **{{CONFIG_NAME}}** preset.

{{ADHD_WARNING}}

---

## 📂 VAULT STRUCTURE

```
{{VAULT_NAME}}/
{{FOLDER_TREE}}
```

### Folder Purposes

{{FOLDER_DETAILS}}

---

## 🛠️ AVAILABLE COMMANDS

### `/setup-vault`
Interactive wizard to configure vault from scratch.

**Usage:** `/setup-vault`  
**Reads config:** No (creates it)  
**Special behavior:** First-time setup only

### `/daily-note`
Create daily note with context from inbox and recent activity.

**Usage:** `/daily-note [date] [--minimal] [--focus project]`  
**Reads config:** Yes (folders.productivity, folders.inbox)  
**Special behavior:** Adapts to your productivity folder location

### `/process-inbox`
Classify and organize notes from inbox.

**Usage:** `/process-inbox [--dry-run] [--auto]`  
**Reads config:** Yes (workflows.inbox.auto_classify)  
**Special behavior:** {{INBOX_BEHAVIOR}}

### `/new-project`
Create new project with structure, brief, and optional research.

**Usage:** `/new-project "project-name" [--no-research] [--minimal]`  
**Reads config:** Yes (folders.projects, frontmatter schema)  
**Special behavior:** Uses your custom frontmatter schema

### `/organize-note`
Move and organize a note to correct location.

**Usage:** `/organize-note path/to/note.md`  
**Reads config:** Yes (all folder paths)  
**Special behavior:** Adapts to your folder structure

### `/link-notes`
Find and create connections between related notes.

**Usage:** `/link-notes "topic" [--auto-link]`  
**Reads config:** Yes (scans configured folders only)  
**Special behavior:** Only searches folders defined in config

### `/project-status`
Show project progress, tasks, and next steps.

**Usage:** `/project-status project-name [--canvas]`  
**Reads config:** Yes (workflows.canvas.enabled)  
**Special behavior:** {{CANVAS_BEHAVIOR}}

---

## 🎨 SKILLS AVAILABLE

Skills installed in `.claude/skills/` (from plugin):

| Skill | Purpose |
|-------|---------|
| `obsidian-markdown` | Wikilinks, callouts, frontmatter, embeds |
| `json-canvas` | Canvas diagram creation and editing |
| `obsidian-bases` | Database-like views (Notion-style) |
| `agent-memory` | Persistent memory between sessions |

**Important:** Read skill documentation before working with these formats.

---

## ⚙️ WORKFLOWS & AUTOMATION

{{WORKFLOWS_SECTION}}

---

## 📝 FRONTMATTER SCHEMA

Every note in this vault should have:

```yaml
---
{{FRONTMATTER_SCHEMA}}
---
```

**Rules:**
- Always add frontmatter to new notes
- Update `modified` field when editing notes
- Use `kebab-case` for `id` field
{{FRONTMATTER_RULES}}

---

## 🔍 SEARCH STRATEGIES

### Strategy
1. **Know the folder?** → Search in specific folder (faster)
2. **Don't know where?** → Search entire vault (ok when necessary)

```bash
# Search in specific folder (preferred when you know location)
grep "keyword" "{{EXAMPLE_FOLDER}}/" --type md -i

# Search entire vault (when you don't know location)
grep "keyword" --type md -i
```

### Common Patterns

| Find | Command |
|------|---------|
| Pending TODOs | `grep "- \[ \]" --type md` |
| By note type | `grep "tipo: {{EXAMPLE_TYPE}}" --type md` |
{{SEARCH_PATTERNS}}

---

{{AGENT_MEMORY_SECTION}}

---

## 🎯 MAIN WORKFLOWS

{{MAIN_WORKFLOWS}}

---

## ⚠️ IMPORTANT RULES

1. **Always add frontmatter** to new notes (required for organization)
2. **Update `modified` field** when editing existing notes
3. **Use Grep before creating** new notes to avoid duplicates
4. **Use kebab-case** for all file names
5. **Ask if ambiguous** about where to place a note
{{ADDITIONAL_RULES}}

---

## 📍 CONTENT TYPES & LOCATIONS

{{CONTENT_MAPPINGS}}

---

## 🔧 CONFIGURATION

This vault's configuration is in: `.claude/vault-config.yml`

**To modify behavior:**
- Edit `.claude/vault-config.yml` (user-editable)
- Commands automatically read this file and adapt
- No need to edit plugin files directly

**Current settings:**
```yaml
automation_level: {{AUTOMATION_LEVEL}}
auto_classify: {{AUTO_CLASSIFY}}
canvas_enabled: {{CANVAS_ENABLED}}
frontmatter_complexity: {{FRONTMATTER_TYPE}}
```

---

## 🆘 TROUBLESHOOTING

### If commands don't work:
```bash
# Verify config exists
cat .claude/vault-config.yml

# Check plugin is enabled
cat .claude/settings.local.json
```

### If auto-classification behaves unexpectedly:
- Edit `.claude/vault-config.yml`
- Change `workflows.inbox.auto_classify` to `false` for manual mode
- Re-run `/process-inbox` to classify with confirmations

{{TROUBLESHOOTING_CONTEXT}}

---

## 📊 PLUGIN INFORMATION

**Plugin:** obsidian-adaptive-plugin  
**Version:** 1.0.0  
**Marketplace:** FrancoEscob/obsidian-plugin-marketplace  
**Documentation:** https://github.com/FrancoEscob/obsidian-plugin-marketplace

**For customization:**
- **End users:** Edit `.claude/vault-config.yml` in your vault
- **Technical users:** See [CUSTOMIZATION.md](https://github.com/FrancoEscob/obsidian-plugin-marketplace/blob/main/CUSTOMIZATION.md)

---

*Generated by /setup-vault wizard*  
*Configuration: {{CONFIG_NAME}}*  
*Last updated: {{DATE}}*

</coding_guidelines>
